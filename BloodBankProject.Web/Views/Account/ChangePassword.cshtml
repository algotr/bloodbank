@model BloodBankProject.Web.ViewModels.ChangePasswordViewModel

@{
    ViewBag.Title = "تغيير كلمة المرور";
}


<div class="container">
    <h2>تغيير كلمة المرور</h2>

    @Html.ValidationSummary(true)

    @using (Html.BeginForm("ChangePassword", "Account", FormMethod.Post, new { role = "form" }))
    {
        <div class="modal-body">
            <div class="form-group">
                @Html.PasswordFor(m => m.CurrentPassword, new { @class = "form-control", placeholder = "كلمة المرور الحالية" })
                @Html.ValidationMessageFor(m => m.CurrentPassword)
            </div>
            <div class="form-group">
                @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "كلمة المرور الجديدة" })
                @Html.ValidationMessageFor(m => m.Password)
            </div>
            <div class="form-group">
                @Html.PasswordFor(m => m.PasswordConfirm, new { @class = "form-control", placeholder = "تأكيد كلمة المرور الجديدة" })
                @Html.ValidationMessageFor(m => m.PasswordConfirm)
            </div>
        </div>
        <div class="modal-footer">
            <button type="submit" class="btn btn-primary">حفظ</button>
            <button type="button" class="btn btn-default" data-dismiss="modal">اغلاق</button>
        </div>
    }
</div>

@section scripts {
    <script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
}